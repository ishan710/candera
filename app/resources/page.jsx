import CanderaResources from '@/components/resources';

export const metadata = {
  title: 'Resources — Candera',
  description:
    'Curated AI resources for founders and teams — from business context to deep engineering.',
};

export default function ResourcesPage() {
  return <CanderaResources />;
}
